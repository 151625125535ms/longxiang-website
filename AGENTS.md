# Agent Instructions

## 1. 总原则

- 所有自然语言输出均使用中文。
- 固定分工：主代理负责设计和执行，并独占文件修改、Git、部署及服务器操作；审查代理和子代理只进行只读分析、测试结果复核和代码审查，不得修改文件或操作服务器。
- 用户最新明确指令优先；如与安全、Git、数据库、服务器规则冲突，按更严格规则执行。
- 不机械执行模糊表达；先理解真实目标，再转成范围清楚、可验证、可回滚的任务。
- 不默认输出“产品经理视角”章节。需要说明业务影响时，直接说明原因、风险和影响即可。
- 用户明确要求“只审查”“只给方案”“不要修改文件”时，只做审查或方案，不得擅自修改、提交、推送或部署。
- 如果无法判断用户是要审查还是执行，优先按审查/方案处理，或用一个简短问题确认。

### 1.1 负优化拦截机制

- 当用户提出的实现路线虽然技术上可行，但可能损害项目长期维护性、数据一致性、后台可维护性、SEO、多语言体系、性能、安全性或部署稳定性时，Codex 不应机械执行。
- Codex 应先区分：
  - 用户目标：用户真正想达到的业务结果；
  - 用户路线：用户提出的实现方式；
  - 项目影响：该路线对系统长期健康的影响。
- 如果用户路线能完成表面目标，但会制造明显技术债，应先提醒用户：
  1. 这条路线可以做；
  2. 它为什么可能让项目变差；
  3. 更推荐的路线是什么；
  4. 如果必须临时采用，应如何限制范围、标记临时性，并安排后续回正。
- 对明显会制造长期债务的方案，例如扩大前端硬编码、绕过后台数据源、重复造一套数据、破坏模块边界、把临时脚本变成正式流程、为单次问题改全局逻辑，Codex 必须暂停并给出替代方案。
- 用户明确坚持低风险临时方案时，Codex 可以执行，但必须在汇报中标注“这是临时方案/技术债”，并给出后续回正路径。
- 不得仅因为某个方案“能实现”“更快”“风险低”就默认采用；完成任务必须兼顾当前效果和项目长期健康。
- 用户不一定知道自己提出的是“目标”还是“实现方式”。Codex 应优先保护用户的真实目标，而不是机械执行用户提出的技术路线。
- 当任务 Prompt 预设的验证、审查或部署矩阵明显超出实际修改影响面，且不涉及高风险边界时，应在执行前简短说明成本和原因，并建议采用最小充分验证；用户明确坚持后仍按最新明确要求执行。该规则不得用于忽略用户指令或削弱高风险保护。

### 1.2 本文件的适用范围

- `AGENTS.md` 只记录跨任务长期有效的项目规则，例如安全边界、部署纪律、架构约束、域名职责和多语言政策；不得为单次任务频繁改写。
- 当前阶段的产品数量、字段白名单、页面清单、执行批次、候选文案和验收矩阵应写入 `task.md`、任务 Prompt 或专项计划，不进入本文件。
- 只有在项目架构、长期业务边界或正式流程发生变化，或同类问题在多个任务中重复出现时，才评估修改本文件。
- 新增规则应可执行、可验证，并避免与已有规则重复或冲突；任务结束后即失效的临时约束不得升级为全局规则。

## 2. 任务分级与执行方式

### 2.1 快速通道：可直接执行

适用于范围清晰、低/中风险、不会写数据库、不改服务器配置、不改认证权限、不明显影响现有线上行为的任务。

常见例子：
- 文案、链接、图片引用、图标、缓存版本号等小改动。
- 前台 JS、页面壳、局部样式的轻量修复。
- 只读校验脚本或验证脚本的小范围增强。
- 明确定位的问题修复，且验证方式清楚。

执行方式：
1. 按第 3 节完成当前任务或连续执行阶段的首次修改前状态检查。
2. 小范围修改，避免混入无关重构。
3. 按影响范围运行验证。
4. 验证通过后精确 `git add`、中文 commit 并 push GitHub；只有影响生产运行的变更才按第 4 节判断是否继续部署及是否需要检查 PM2。

快速通道不需要冗长方案；最终汇报说明改了什么、验证结果、commit/push/pull 状态即可。

### 2.2 标准通道：简短方案后执行

适用于跨多个模块、公共大文件实质修改、新增接口、新增后台模块、SEO/i18n 开关、可能影响多页面的任务。

要求：
- 先简短说明目标、影响范围、风险、验证方式。
- 用户已经明确授权执行且风险可控时，可以连续完成同一阶段内的低/中风险步骤，不必逐项等待确认。
- 每个可提交阶段只处理一类问题，以可验证结果为边界，不要为了流程拆成过碎编号。

### 2.3 高风险通道：必须暂停确认

以下情况必须先说明风险、实施方案、备份、回滚和验证方式，等待用户确认后再执行：

- 生产数据库写入或数据库结构变更。
- 后台权限、认证、登录流程变更。
- 服务器配置、PM2、Nginx、环境变量变更。
- 生产运行状态、服务重启策略或依赖安装变化。
- 新语言正式启用、sitemap/hreflang/noindex 重大切换。
- 超出用户已确认的字段、页面或语言范围，并可能改变现有英文/阿语线上行为。
- 需要处理与当前任务无关的用户改动或生产已有改动。
- 单阶段范围已经难以验证、难以回滚，或风险无法清楚界定。

## 3. 修改前检查与用户改动保护

- 每个任务或连续执行阶段首次修改前必须执行 `git status --short`，提交前再次确认。只有阶段切换、检测到外部文件变化、长时间中断恢复或准备执行高风险动作时才重新检查；连续阶段内状态未变化时，无需在每次 `apply_patch` 前重复执行。
- 如存在用户未提交改动，判断是否与当前任务相关；不得回滚、覆盖或删除用户已有改动。
- `.tmp/`、`chanpince/`、`docs/superpowers/plans/*` 等既有未跟踪噪音默认不处理，除非用户明确要求。
- 修改范围尽量小，避免把无关格式化、历史清理、治理任务或重构混入当前需求。
- 如果发现执行中风险升级，应停止当前扩大动作，先汇报原因和下一步建议。

## 4. Git 与生产部署

- 项目仓库内的代码和正式文档变更必须完成：本地修改 -> 本地验证 -> 精确 `git add` -> 中文 commit -> `git push origin main`。
- 影响生产运行的代码、依赖、静态资源或运行时配置模板，在用户已授权执行和部署且验证通过后，继续执行服务器 `git pull origin main`；高风险事项仍按第 2.3 节单独确认。
- 纯文档、审计记录、开发期测试资料和不被生产运行时调用的辅助文件，默认只需提交并 push，不执行服务器 pull，也不检查或重启 PM2。
- 桌面报告、临时审查文档除非用户要求，不进入 Git。
- 默认直接提交到 `main`。大型治理、重构或并行任务可以使用临时分支或 worktree，但最终必须合回 `main`，服务器只允许 pull `main`。
- GitHub 是代码权威来源；本地和服务器都应以 GitHub 为同步基准。
- 禁止执行 `git push longxiang` 或任何向生产服务器 Git remote 推送代码的命令。
- 服务器只允许 pull，不得直接编辑代码、静态页面、脚本、数据库结构或配置模板。
- 如果 `package.json` 或 `package-lock.json` 发生变化，服务器 pull 后按实际情况安装依赖，并检查 PM2。
- 后端运行时代码、依赖、启动参数或服务异常涉及进程状态时，需要检查 PM2。只有后端代码必须重新加载、依赖变化、启动参数变化或服务异常时，才按用户授权和高风险规则考虑重启，并说明原因。
- 纯静态 HTML、CSS、图片和浏览器端资源部署后，以真实生产页面和资源请求作为主要验收证据；不影响后端进程加载、依赖或启动参数且服务无异常迹象时，不额外检查或重启 PM2。

## 5. 验证规范

- 验证采用与风险、调用面和修改内容匹配的最小充分证据，必须覆盖实际修改行为及最可能受影响的直接调用方，不得机械运行与修改无关的全量命令。公共模块、跨页面逻辑和高风险变化仍须按实际影响范围扩大验证。
- 任务完成标准以用户目标是否在实际使用场景中达成为准，不以“已修改代码”“已提交”“已部署”本身作为完成依据。
- 对前台页面、后台页面、视觉、交互、多语言文案、SEO 展示等用户可见任务，必须以真实页面、真实接口或真实运行结果作为最终验收依据。
- 如果代码已修改、验证命令已通过，但真实页面或接口仍未达到用户目标，不得宣布任务完成，应继续定位原因或明确列出剩余阻断项。
- 汇报时应区分已完成的代码/配置/数据变更、已通过的验证、真实页面或接口仍存在的问题，以及是否可以进入下一阶段。
- 不得用“脚本通过”“blockers=0”“已 commit/push/pull”替代真实结果验收。
- 普通 JavaScript 修改：运行 `node --check` 检查相关文件。
- 后台纯 CSS、图标、展示结构以及不影响接口和保存逻辑的局部 UI 修改，只进行相关静态检查、目标后台页面真实验收和必要视口检查，不因文件位于 `admin/` 目录而机械运行完整 acceptance。
- 修改后端路由、公开或后台接口、保存逻辑、认证、权限、数据库结构、数据库读写或前后台数据联动时，运行相关文件语法检查、定向验证和 `node scripts/test-acceptance.js`；改变提交 payload、接口契约或数据保存结果时，必须完成前后台闭环验证。
- 现有正式语言中的单条文案、精确映射或内容字段修复，如不改变语言配置、路由、页面壳、索引状态、canonical、hreflang、sitemap 或共享回退逻辑，只运行相关语法或数据检查、目标页面真实验收，并按直接调用方抽查必要基线语言，不机械运行 sitemap 和全量 SEO/i18n 校验。
- 修改语言配置或正式启用状态、路由、语言选择器、页面壳、共享回退逻辑、canonical、hreflang、x-default、noindex/index 或 sitemap 时，运行 `node scripts/generate-sitemap.js --dry-run` 和 `node scripts/verify-seo-i18n.js`，并按实际影响范围检查原始 HTML 或真实页面；重大切换仍按高风险通道执行。
- 前台视觉或交互修改使用浏览器或 Playwright 验证，默认只覆盖直接受影响的页面、状态、语言和视口，并能说明选择范围与实际调用面的关系。文案长度可能影响布局时补充相关窄视口；只有公共布局、响应式 CSS、RTL、导航、共享组件、全局语言逻辑、公共运行时或跨页面行为变化时才扩大矩阵，不得机械覆盖所有页面、语言和视口组合。
- 仓库内文档或流程修改至少执行 `git diff --check`。仓库外桌面报告只验证文件存在、编码、内容结构和用户要求，不承担 Git、部署或 PM2 流程。
- 依赖修改：运行 `npm audit --omit=dev`，并说明剩余风险。
- 基线记录以区分本次新增失败为目的。只有存在与当前修改相关、可比较且来源明确的前置证据时，才能把失败认定为既有问题；本次修改不得新增或恶化问题，没有可靠基线时不得无证据降级新失败。
- 与任务无关的普通既有失败应记录但不自动阻断低/中风险任务；生产安全、数据一致性、认证权限、敏感信息或数据损坏等严重问题仍可以阻断。快速任务不要求把昂贵全量命令在修改前后机械运行两次，也不得为了让无关检查变绿而修改本地或生产业务数据。
- 出现本次新增或恶化的验证失败时，先在当前阶段内小范围修复；如果需要越界修改或风险升级，暂停并汇报。完成最小充分验证并确认不存在本次新增或恶化的阻断问题后，才能执行 `git add`、`git commit`、`git push` 和服务器 `git pull`。

## 6. 数据库、资源与敏感信息

- 禁止提交 `.env`、数据库文件、密钥、后台密码、SMTP 密码、服务器私钥、访问令牌等敏感信息。
- `data/*.db`、`data/*.db-shm`、`data/*.db-wal`、`uploads/` 不进入 Git。
- 运行时生成文件、临时文件、截图、OCR 结果、备份文件和大体积原始资料不得混入提交。
- 新增环境变量时，必须同步更新 `.env.example`，并说明用途、示例值和是否必填。
- 数据库结构变更必须可复现，并先准备备份、迁移方式和回滚方式。
- 涉及产品、分类、证书、content_blocks 等业务数据判断时，以生产环境当前数据为准；本地数据只作为验证副本。
- 生产数据库写入必须先 dry-run、备份、明确写入范围和验证方式；不得顺手处理无关数据。
- 一般性的“执行任务”或“部署代码”授权不自动包含生产数据库写入；生产数据库写入必须针对明确的数据范围单独确认。
- 生产数据库结构不得在服务器上手工修改，必须通过本地代码、迁移脚本或明确可复现步骤完成。

## 7. 模块化边界

- 默认保持模块职责清晰，避免为了方便继续膨胀公共大文件。
- 公共文件可以修改，但新增内容应属于入口初始化、全局注册、路由挂载、公共 UI 行为、公共工具或跨模块复用逻辑。
- 页面或业务专属逻辑优先放到对应模块：
  - 产品列表：`js/products-list.js`
  - 产品详情：`js/product-detail.js`
  - Education 页面：`js/education.js`
  - 内容页：`js/content-pages.js`，复杂时评估拆分
  - 后台业务模块：`admin/js/modules/*`
  - 后台入口：`admin/js/admin.js` 只负责初始化、模块注册和调度
  - 服务入口：`server/app.js` 只负责启动、中间件和路由挂载
  - 具体接口：`server/routes/*` 或 `server/routes/admin/*`
  - 可复用后端能力：`server/lib/*` 或合适的 service/helper
- 当前项目尚未完成 CSS 拆分前，允许在 `css/styles.css` 做命名空间清楚的小范围样式修改。新增约 30 行以上、涉及多个断点、全局变量、基础组件或跨页面样式时，需要说明模块化判断。
- 新增文件必须职责清楚、有明确调用方和长期复用价值。不得为了绕开公共文件限制而制造职责不清的小文件。
- 不重复实现已有公共能力；涉及工具函数、弹窗、请求封装、资源路径处理、上传逻辑等公共能力前，应先搜索现有代码。
- 修改公共大文件时，最终简短说明本次修改属于公共职责还是临时兼容、是否影响现有调用方，以及是否存在后续拆分风险。

## 8. 多语言与 SEO 特别规则

- `lxenelectric.com` 是全球官网，后续网站修改和 SEO 优化只针对该站执行。
- `lxelec.cn` 是中国官网，只允许基于公开信息进行只读观察；不得对其执行修改、迁移、301、canonical、noindex、hreflang、内容调整或站长平台配置。
- `.com` 不得公开国内销售电话或国内邮箱；在国际电话或 WhatsApp 正式提供前，只使用经确认的国际联系渠道。
- 不修改 Hero 图片，不降低 Hero 图片的显示清晰度；涉及 Hero 资源的性能方案必须先确认不会损害视觉质量。
- 不修改 Contact 页现有地址标签；如需改变地址结构或标签文本，必须单独确认。
- `config/locales.json` 是语言状态的权威配置。
- planned locale 不得提前进入 `supportedLocales`、语言选择器、sitemap 或 hreflang。
- 新语言正式启用必须同时验证页面壳、canonical、alternate、x-default、sitemap、noindex/index、前台路径和产品详情路由。
- 英文和阿语是既有线上基线；共享语言逻辑、页面壳、路由、SEO 或可能改变其现有线上行为的修改必须重点验证。现有正式语言的内容级修复按第 5 节收敛验证范围；用户确认字段级或页面级变更范围后，可在该范围内连续执行，新增字段、扩大页面或产品范围、改变页面结构或调整语言索引状态时必须重新确认。
- sitemap URL count 应使用数据感知校验，不回到硬编码固定值。
- pt 如仍为 planned，只能保持预留状态，不得被语言选择器、sitemap、hreflang 或正式路由提前暴露。

## 9. 项目治理任务

- 治理类任务必须分阶段、小批量提交。
- 每阶段只处理一类问题，例如安全依赖、环境配置、资源清理、多语言结构、SEO 校验、后台拆分或样式拆分。
- 不得一次性同时修改安全依赖、多语言架构、后台结构、样式系统和部署流程。
- 治理类任务优先先做计划，再执行第一阶段；每阶段完成后执行必要验证、提交并 push，是否服务器 pull、部署或检查 PM2 统一按第 4 节的生产运行影响和用户授权判断。不得因为任务被称为“治理任务”就自动执行完整生产部署。
- 但不要为了流程把任务拆得过碎；以“可验证的结果阶段”为单位，大阶段内的低/中风险小步骤可以连续执行。

## 10. Superpowers 与插件使用

- 可以使用 superpowers、skills 和插件提升质量，但不得覆盖本文件规则和用户明确指令。
- 使用前简短说明用途即可，不要把“技能/插件适用性判断”写成冗长章节。
- 代码审查 agent、子代理或额外插件适合复杂任务、用户明确要求或风险较高的任务；普通小改不强制使用，也不得为了形式完整叠加没有实际审查价值的 agent、skills 或重复验证。
- 当 superpowers 流程建议与本文件冲突时，以本文件为准。

## 11. Windows 命令执行约定

- 本地 Windows 项目命令优先使用 PowerShell 7（`pwsh`）；执行前应确认其可用。不可用时，可以使用当前 PowerShell 环境完成简单、低风险操作，不得因 Shell 偏好阻塞任务。
- Git、文件发现、状态检查和 Windows 原生操作优先使用 PowerShell。复杂 SSH、远程 Bash 脚本和多行 Linux 命令，在确认 Git Bash 可用时优先使用 Git Bash；否则通过 SSH 标准输入传递脚本。
- 涉及复杂 JSON、SQLite、文本转换或批量数据检查时，优先复用项目现有脚本或使用结构化 Node.js 脚本。简单且不存在多层转义的 JSON 操作可以使用 PowerShell 原生结构化命令。
- 中文文本文件必须使用能够可靠按 UTF-8 解码的工具读取，例如 `rg`、Node.js UTF-8 文件 API 或 `Get-Content -Encoding utf8`。终端显示乱码时，不得据此判断源文件损坏。
- 手工文件修改统一使用 `apply_patch`。项目已有的生成器、迁移工具、格式化器和机械批处理可以按其正式流程写入文件，但执行后必须检查 diff、编码和输出范围。不得使用可能隐式改变编码的 Shell 重定向、`Set-Content` 或跨编码复制方式手工修改正式文件。
- 禁止把 PowerShell、SSH、Bash、Node.js 和 SQL 嵌套成复杂单行命令。远程复杂逻辑应优先通过标准输入或已有项目脚本传给服务器执行；在 Git Bash 或远程 Bash 环境中可以使用 here-document。
- 同一种引号、转义或路径方案失败一次后，应切换到标准输入、职责明确的临时只读检查脚本或已有项目命令，不得反复微调脆弱命令。
- 临时远程检查脚本优先通过 SSH stdin 执行，不在生产服务器落地。确需生成本地临时文件时，应放入既有临时目录、不得提交，并在删除前核对绝对路径。
- 优先复用 `package.json` 和 `scripts/` 中已有命令。重复出现且具有长期价值的检查应沉淀为职责清楚的只读脚本，避免反复拼接临时命令。
- 状态检查、代码验证、数据库检查和生产检查原则上按目标分别执行，避免局部失败掩盖后续结果；项目已有且能够清楚报告各阶段结果的组合命令可以继续使用。
- 同一次文件移动、删除或目录操作必须由同一种 Shell 从路径解析到执行全程完成，不得在 PowerShell 与 Git Bash 之间传递计算后的删除或移动路径。
- Windows 下的递归删除、移动和目录清理优先使用 PowerShell 原生命令及 `-LiteralPath`。执行前必须解析并核对目标绝对路径，确认其位于预期工作区或用户明确指定的目录内。
- Git Bash 路径使用 `/c/`、`/d/` 等格式；PowerShell 路径使用 `C:\`、`D:\`。不得在未转换和核验的情况下混用。
- 复杂生产查询优先使用项目内已有只读脚本。必须临时执行时，通过 stdin 将脚本传入 SSH，不使用多层 `node -e`、SQL 和 Shell 引号拼接。
- 长时间运行的命令必须持续跟踪其进程或会话；能够获得会话标识时必须保留。任务结束前不得遗留状态不明或已不再需要的进程；确需持续运行的开发服务器或守护进程，必须核验状态，并在最终汇报中说明地址、进程或会话状态。
