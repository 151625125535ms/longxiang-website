# Agent Instructions

## 语言规范

所有自然语言均使用中文。


## 协作协议

- 固定分工：Codex 设计并执行，Claude 只审查方案，不执行任务、不修改文件。

## 需求理解与协作方式

- 本节是协作方式补充，不覆盖本文件中的 Git、验证、安全、数据库、服务器和模块化规则；如有冲突，按更严格规则执行。
- 用户可能使用非专业、模糊或不准确的表达，且对技术细节不一定了解。
- Codex 不应机械执行字面要求，而应先以产品经理视角理解真实目标、识别潜在误区，并把需求翻译成可执行、可验证的具体任务。
- 当用户前后指令不一致时，以最新明确指令为准。
- 当用户明确要求执行，或按上下文可明确判断用户希望完成落地修改，且无实质性风险、范围清晰、影响可控时，Codex 应主动完成代码修改、验证、精确提交、推送 GitHub 和服务器 pull。
- 当用户明确要求“先审查”“先给方案”“不要修改文件”时，Codex 只做审查或方案，不得擅自修改、提交、推送或部署。
- 如果 Codex 无法明确判断用户是要审查还是要执行，应先按审查/方案处理，或用简短问题确认。
- 对存在实质性风险的任务，例如会改变数据库结构或数据、影响认证与权限、修改服务器配置、改变生产运行状态，或一次性跨多个模块且优先级不清的任务，应先给出风险判断、实施方案和验证方式，待确认后再执行。
- 对文字、样式、链接、图片引用、后台展示布局等低风险且范围清晰的小改动，在用户要求执行或上下文明确需要落地修改时，可直接按本地修改、验证、提交、推送、服务器 pull 流程执行。
- Codex 应避免把无关重构、格式化、历史清理或治理任务混入当前需求。
- 回复应使用通俗中文，避免假设用户具备技术背景；解释技术决策时，需用简单明了的语言说明原因和影响。

## Git 管理规范

### 代码修改流程

所有代码变更必须遵循以下顺序，**禁止直接在远端服务器上修改代码**：

1. 在本地开发环境完成代码修改
2. 本地测试验证通过
3. 提交到本地 git（`git add` → `git commit`）
4. 推送到 GitHub（`git push`）
5. 远端服务器从 GitHub 拉取更新（`git pull`）

### 核心原则

- **远端服务器只做 pull，不做编辑**：生产服务器上不得直接修改任何文件，所有变更必须经过本地 → GitHub → 服务器的完整链路
- **GitHub 是唯一代码源**：GitHub 仓库是项目的权威备份，本地和服务器的代码状态均以 GitHub 为准
- **每次功能完成后及时 push**：避免本地堆积大量未推送的提交，保持 GitHub 与本地同步。push 到 GitHub 和服务器 pull 均由执行模型（Codex）在工作流中完成，无需用户手动操作

### 补充规范

- **分支策略**：默认直接提交到 `main`。小改动直接在 `main` 执行。大型治理、重构或并行审查任务，可以使用临时本地 worktree 或临时分支隔离风险，但最终必须合并回 `main`，并且服务器只允许 pull `main`。
- **commit 信息语言**：备注统一使用中文，简明描述本次改动内容（如“修复产品页动画空白”、“更新首页 Hero 文案”）
- **全流程由模型执行**：commit、push 到 GitHub、服务器 pull 均由 Codex 在工作流中自动完成，用户无需手动执行任何 git 命令


## 生产部署与服务器规范

- **禁止向服务器 remote 推送代码**：不得执行 `git push longxiang` 或任何向生产服务器 Git remote 推送代码的命令。
- **服务器只允许 pull**：服务器更新只能通过 SSH 登录服务器后，在项目目录执行 `git pull origin main`。
- **服务器不得直接编辑文件**：包括代码、静态页面、样式、脚本、数据库结构和配置模板。所有变更必须从本地提交到 GitHub 后再由服务器拉取。
- **依赖变化需同步处理**：如果 `package.json` 或 `package-lock.json` 发生变化，服务器拉取后必须按实际情况执行依赖安装，并检查 PM2 服务状态。
- **运行状态需检查**：涉及线上代码或依赖更新后，需要检查 PM2 服务状态；必要时重启对应服务。


## 修改前检查

- 每次修改前必须执行 `git status --short`，确认当前工作区状态。
- 如存在用户未提交改动，必须判断是否与当前任务相关；不得回滚、覆盖、删除用户已有改动。
- `.tmp/`、`chanpince/` 等未跟踪目录默认不处理，除非用户明确要求。
- 修改范围应尽量小，避免把无关格式化、历史清理或重构混入同一次任务。


## 验证规范

- 普通 JavaScript 修改：运行 `node --check` 检查相关文件。
- 后端、后台、接口、鉴权、数据库相关修改：运行 `node scripts/test-acceptance.js`。
- 依赖修改：运行 `npm audit --omit=dev`，并说明剩余风险。
- 页面视觉修改：需要进行浏览器、截图或 Playwright 验证。
- 文档或流程修改：至少执行 `git diff --check`，确认没有空白或格式异常。
- 验证通过后才能执行 `git add`、`git commit`、`git push` 和服务器 `git pull`。


## 环境变量与敏感信息

- 禁止提交 `.env`、数据库文件、密钥、后台密码、SMTP 密码、服务器私钥、访问令牌等敏感信息。
- 新增环境变量时，必须同步更新 `.env.example`，并说明用途、示例值和是否为必填项。
- 生产环境变量只允许配置在服务器环境或服务器 `.env` 文件中，不得硬编码到代码里。
- 涉及后台账号、JWT、SMTP、Cloudflare、AWS、GitHub 等凭据时，只能引用变量名，不得在回复或提交内容中明文暴露真实值。


## 数据库与上传资源

- `data/*.db`、`data/*.db-shm`、`data/*.db-wal`、`uploads/` 不进入 Git。
- 数据库结构变更前必须先备份，并说明迁移方式和回滚方式。
- 不得直接在生产服务器手工修改数据库结构；数据库 schema 变化必须通过本地代码、迁移脚本或明确的可复现步骤完成。
- 上传资源、运行时生成文件、临时文件、截图、OCR 结果和大体积原始资料不得混入代码提交。


## 模块化边界规范

本规则适用于项目中所有前端 JS/CSS 和后端 Node.js 代码修改。目标是保持模块职责清晰，避免为了方便把页面专属、业务专属或复杂逻辑继续堆进公共大文件。模块化不等于强制“一个功能只能有一个文件”，而是要求职责清楚、归属合理。

### 默认原则

- 开发时应保持模块化边界清晰，避免无必要地扩大已有公共大文件。
- 公共文件包括但不限于：
  - `js/main.js`
  - `js/content-pages.js`
  - `css/styles.css`
  - `admin/js/admin.js`
  - `admin/js/admin-api.js`
  - `admin/js/admin-ui.js`
  - `server/app.js`
  - `server/lib/*` 中承担跨路由复用能力的文件
  - 公共 utils / common / shared 文件
- 公共文件可以修改，但新增内容应优先属于：
  - 应用级入口初始化；
  - 全局模块注册；
  - 路由挂载；
  - 全站公共 UI 行为；
  - 全站公共样式变量、基础样式、通用组件样式；
  - 请求封装、资源路径处理、弹窗、提示、分页、表单基础工具等公共能力；
  - 已被至少 2 个独立页面/模块复用，或从职责上明显属于公共层的能力。
- 页面专属、业务专属、临时兼容、单一模块复杂逻辑，不应继续塞入公共大文件。
- 当前项目尚未完成 CSS 文件拆分前，允许在 `css/styles.css` 中进行页面或区块命名空间明确的小范围样式修改。样式选择器应尽量限定在 `.hero`、`.product-detail-redesign`、`.product-directory-section`、`.page-*` 等页面/区块作用域下。单次新增超过约 30 行、涉及多个断点、修改全局变量/基础组件，或可能影响多个页面时，需要进行简短模块化判断，并在最终回复中说明为何暂不拆分及是否存在后续拆分风险。

### 归属规则

- Products 页面逻辑优先放在 `js/products-list.js` 或产品相关模块中。
- Product Detail 页面逻辑优先放在 `js/product-detail.js`。
- Education 页面逻辑优先放在 `js/education.js`。
- Contact / About / Solutions 等内容页逻辑，如较复杂，应评估是否从 `js/content-pages.js` 拆出页面专属模块。
- 后台模块专属逻辑优先放入 `admin/js/modules/*`，例如：
  - 产品管理：`admin/js/modules/admin-products.js`
  - 询盘管理：`admin/js/modules/admin-inquiries.js`
  - 资源库：`admin/js/modules/admin-assets.js`
  - 可视化管理：`admin/js/modules/admin-visual-editor.js`
  - 教育合作：`admin/js/modules/admin-education.js`
- `admin/js/admin.js` 只应负责后台入口初始化、模块注册和页面级调度，不应承载具体业务渲染、接口细节或复杂状态逻辑。
- `server/app.js` 只应负责应用启动、中间件挂载和路由挂载。
- 具体接口逻辑应放在 `server/routes/*` 或 `server/routes/admin/*`。
- 可复用业务能力、文件处理、资源处理、缩略图生成、数据库辅助逻辑，应放入 `server/lib/*` 或更合适的 service/helper 文件中。
- 如果本次需求涉及的逻辑已存在于公共大文件中，且从职责上属于页面或模块专属，应优先评估是否迁移到对应页面文件或业务模块。若迁移超出当前任务范围、风险较高，或会引入不必要的大范围变更，可以暂不迁移，但需在最终回复中说明原因，并给出后续拆分建议。

### 新增文件规则

- 新增文件必须有清晰职责、明确调用方和可预期复用价值。
- 不得为了绕开公共文件限制而制造职责不清的小文件。
- 模块化不等于强制“一个功能只能有一个文件”。当功能复杂度较高时，可以拆成同一模块目录或同一职责组下的多个文件；当只是小范围样式、文案、图标、链接、缓存版本号等轻量改动时，可以在现有合理文件中完成。
- 新增文件如果不在已有模块目录内，例如直接在 `js/`、`admin/js/` 下新建孤立的 `utils.js`，必须在最终回复中说明：为什么现有目录无法容纳、该文件的长期复用预期是什么，以避免零散文件堆积。

### 禁止行为

- 为了方便，把某个业务功能的渲染、状态、接口细节塞进入口文件或全局文件。
- 为了避免修改公共文件，把本应属于公共层的能力复制到模块内。
- 为了避免修改公共文件，在全局范围新建职责不清的小文件。
- 重复实现已有公共能力。
- 在涉及工具函数、弹窗、请求封装、资源路径处理、上传逻辑等公共能力前，不搜索现有代码就直接重复实现。

### 触发模块化审查的情况

以下情况需要在修改前进行简短模块化判断，并在最终回复中说明职责归属：

1. 新增后台功能模块。
2. 新增前台页面或大型页面区块。
3. 新增后端接口或重要业务逻辑。
4. 对 `js/main.js`、`js/content-pages.js`、`admin/js/admin.js`、`server/app.js` 等公共大文件进行实质性修改，例如新增公共能力、入口逻辑、路由挂载或复杂状态逻辑；不包含纯注释、变量名修正或简单文案调整。
5. 对 `css/styles.css` 新增样式超过约 30 行，或修改全局变量、基础组件、跨页面样式，或可能影响多个页面。
6. 单次实质性代码改动预计超过 3 个文件，或会影响多个页面；不包含缓存版本号、静态引用同步、简单文案、链接或图标修正。
7. 发现现有文件已明显过大，且本次需求会继续增加复杂逻辑。

如果只是小范围样式、文案、链接、图标、缓存版本号等轻量改动，可以直接在现有合理文件中完成，不需要展开完整模块化分析。最终回复只需简要说明影响范围。

### 修改公共大文件时的说明要求

如果确需修改公共大文件，请在最终回复中简要说明：

- 本次修改属于入口调度、公共样式、公共组件、公共工具，还是跨模块复用逻辑。
- 为什么放在公共文件中比放在具体模块中更合理。
- 是否会影响现有调用方。
- 是否存在后续拆分风险。

只有当代码本身不容易理解，或未来维护者需要上下文时，才在代码附近添加简短注释。


## 项目治理任务规范

- 项目治理必须分阶段、小批量提交。
- 每一阶段只处理一类问题，例如安全依赖、环境配置、资源清理、多语言结构、SEO 校验、后台拆分或样式拆分。
- 不得一次性同时修改安全依赖、多语言架构、后台结构、样式系统和部署流程。
- 治理类任务优先先做计划，再执行第一阶段；每阶段完成后必须验证、提交、推送并同步服务器。
- 使用 superpowers 插件时，不能覆盖本文件规则；本文件优先级高于插件流程建议。
