# Handoff Template

```text
给 Claude 的提示词

# Codex 方案审核请求

## 任务目标

## 必读文档

## 持久化任务文档

## Codex 初版方案

## 本轮范围

## 明确不做

## 允许修改的文件或数据

## 禁止修改的文件/接口

## 计划步骤

## 验收标准

## 已检查的文件范围

> 列出本次改动涉及的所有文件，以及主动 grep 检查的调用点。
> 格式：`文件名` — 检查内容（如：grep initScrollAnimations 全项目，发现 N 处调用）

## 视觉对照

> 改动前后的截图列表，逐区块说明变化。
> 格式：
> - 区块名称：改动前状态 → 改动后状态
> - 若区块消失：说明原因（是否属于预期）

## 视觉自评

> Codex 对改动后页面视觉效果的主观评估（不只是功能验证）。
> 必须包含：整体视觉是否与网站调性一致、是否有新增的视觉异常。

## 未覆盖场景

> 列出自动化测试（Playwright）未覆盖的关键页面和交互场景，作为 Claude 审查的潜在盲区提示。

## 规格文件更新

> 若本次涉及方案调整（Claude 审查意见或用户反馈），确认是否已将调整合并回 `docs/tasks/{task-id}-spec.md`。
> 填写：已更新（版本号） / 无调整（原因）

## 需要 Claude 审核的问题

- 方案是否可执行
- 是否违反代码边界或专项约束
- 是否遗漏关键文件、数据、语言版本或验证步骤
- 是否存在更低风险的实施顺序
- 是否需要拆分任务
- 视觉对照是否完整，是否存在区块消失或内容异常未解释的情况

## 审核完后

请将审核意见写入 `C:\Users\hnlxd\Desktop\codex_check.md`，由用户转交 Codex。
Claude 只负责审查方案，不执行任务、不修改文件、不运行数据迁移。
```
